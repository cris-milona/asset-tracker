// Centered roughly on the seed data's northeast-US spread, used whenever there's no asset location yet to center on (e.g. picking a location for a brand-new asset).
export const DEFAULT_MAP_CENTER: [number, number] = [41.5, -80];
export const DEFAULT_MAP_ZOOM = 5;
